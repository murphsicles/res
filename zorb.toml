[package]
name = "@overlay/res"
version = "0.1.0"
edition = "2026"
description = "Resilience and state recovery — bounded backoff, circuit breaker, idempotent resubmission"
license = "MIT OR Apache-2.0"
repository = "https://github.com/murphsicles/res"
keywords = ["overlay", "bsv", "cryptography", "security"]
categories = ["cryptography"]
